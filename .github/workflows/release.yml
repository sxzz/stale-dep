name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@v1
    with:
      publish: true
      install: pnpm install && pnpm run update-hash
    permissions:
      contents: write
      id-token: write
